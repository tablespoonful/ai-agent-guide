制御開始直後にJ7のjerkが上限にヒットしています。

開始時のtrajectory generator内部状態を、実機の現在のcommanded state（q_d, dq_d、可能ならddq_d）で初期化してください。

BCの最初のtargetへ直接ジャンプさせず、現在のcommanded stateからjerk-limited S-curveで連続接続してください。

特にJ7について、開始直後100〜500msの q / dq / ddq / jerk とEE姿勢targetをログ化し、初期値不連続またはIKによる急変がないか確認・修正してください。

jerk上限を緩和することで回避しないでください。